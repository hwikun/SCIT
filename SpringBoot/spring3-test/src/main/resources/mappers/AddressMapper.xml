<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.spring3test.dao.AddressDAO">
<insert id="createAddress" parameterType="Address">
	insert into address values(#{name}, #{address}, #{zipcode}, #{phone})
</insert>
</mapper>